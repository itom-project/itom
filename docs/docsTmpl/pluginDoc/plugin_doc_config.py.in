#configuration file for itom plugin ${PLUGIN_NAME}
#-------------------------------------------------

pluginDocTarget       = "${PLUGIN_NAME}"
pluginDocSourceDir    = "${PLUGIN_DOC_SOURCE_DIR}"
pluginDocBuildDir    = "${PLUGIN_DOC_BUILD_DIR}"
pluginDocInstallDir   = "${PLUGIN_DOC_INSTALL_DIR}"
pluginDocMainDocument = "${PLUGIN_DOC_MAIN}"